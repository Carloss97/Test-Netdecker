{
  "buildCommand": "npm --prefix frontend ci --include=dev --no-audit --progress=false && npm --prefix frontend run build",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
